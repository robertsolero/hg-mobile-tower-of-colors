using UnityEngine;

namespace Submodule.Missions
{
    /// <summary>
    /// used by Mission UI popups as a target, this allows us to find a specific transform on scene to use as parent.
    /// </summary>
    public class MissionUIPopupParentTarget : MonoBehaviour
    {
    }
}